import React from 'react'

export default function Message({ message, toggleMessage }) {
    function handleMessageClick() {
    }
    return (
        <div>
            <label>
                {message.name}
            </label>
            
        </div>
    )
}
